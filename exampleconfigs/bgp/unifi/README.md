The config.conf file is a BGP configuration file for FRRouting (FRR), which is an open-source routing software suite. This configuration sets up BGP (Border Gateway Protocol) with specific parameters and neighbors.

This file is the format Unifi uses for BGP configuration when you enable BGP in the Unifi Network application.